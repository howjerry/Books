# 附錄 D：Skills 開發最佳實踐檢查清單

> 本附錄提供 Skills 開發的完整檢查清單，涵蓋設計、實作、測試、部署的各個階段。

---

## 📋 Skills 設計檢查清單

### ✅ 職責單一性

- [ ] Skill 只做一件事，且做好
- [ ] 如果需要多個步驟，考慮拆分為多個 Skills
- [ ] Skill 名稱清晰表達其用途
- [ ] Description 在 2 句話內說明核心功能

### ✅ 可重用性

- [ ] Skill 可在不同場景下使用
- [ ] 避免硬編碼特定值
- [ ] 使用參數化配置
- [ ] 提供合理的預設值

### ✅ 介面設計

- [ ] 參數命名清晰一致（使用 snake_case）
- [ ] 必要參數最少化
- [ ] 可選參數有預設值
- [ ] 返回值結構化且一致

**範例**：

```markdown
## Parameters

### url (required)
- Type: string
- Description: Target website URL
- Example: `"https://example.com"`

### timeout (optional)
- Type: integer
- Default: 30
- Range: 5-300
- Description: Request timeout in seconds
```

---

## 🎯 SKILL.md 撰寫檢查清單

### ✅ 必要章節完整

- [ ] `# Skill Name` - 清晰的標題
- [ ] `## Description` - 簡潔的功能描述
- [ ] `## When to use` - 具體的使用場景
- [ ] `## Parameters` - 完整的參數定義
- [ ] `## Returns` - 明確的返回值說明
- [ ] `## Examples` - 實際的使用範例

### ✅ Description 品質

- [ ] 1-2 句話說明核心功能
- [ ] 使用主動語態
- [ ] 避免模糊用詞（如 "處理"、"管理"）
- [ ] 包含關鍵特性或限制

**✅ 好的範例**：
```markdown
## Description
Performs comprehensive website health checks including HTTP status validation,
response time measurement, and SSL certificate verification.
```

**❌ 不好的範例**：
```markdown
## Description
This skill checks websites.
```

### ✅ When to use 具體性

- [ ] 列出 3-5 個具體場景
- [ ] 使用動詞開頭（Verify, Monitor, Validate）
- [ ] 包含上下文資訊
- [ ] 幫助 Claude 快速判斷適用性

**範例**：
```markdown
## When to use
- Verify website accessibility after deployment
- Monitor production services in CI/CD pipelines
- Validate API endpoints before release
- Check SSL certificate expiration
- Measure website response time for SLA monitoring
```

### ✅ Parameters 完整性

每個參數必須包含：

- [ ] 名稱（snake_case）
- [ ] 必要性（required/optional）
- [ ] 類型（string, integer, boolean, array, object）
- [ ] 描述（清晰說明用途）
- [ ] 約束條件（範圍、格式、長度）
- [ ] 範例值（實際可用的例子）
- [ ] 預設值（如果是 optional）

### ✅ Examples 實用性

- [ ] 至少提供 2 個範例（基本 + 進階）
- [ ] 包含輸入和預期輸出
- [ ] 範例可以直接執行
- [ ] 涵蓋常見使用模式
- [ ] 展示錯誤處理

---

## 💻 代碼實作檢查清單

### ✅ Python Skills

#### 結構

- [ ] 使用類別封裝邏輯
- [ ] 提供 `execute_skill()` 入口函數
- [ ] 分離驗證、執行、格式化邏輯
- [ ] 適當使用 async/await

#### 類型註解

- [ ] 所有參數有類型註解
- [ ] 返回值有類型註解
- [ ] 使用 `typing` 模組（Dict, List, Optional）
- [ ] 複雜類型使用 Pydantic Model

#### 錯誤處理

- [ ] 參數驗證拋出 `ValueError`
- [ ] 執行失敗拋出 `RuntimeError`
- [ ] 不吞沒異常（避免 bare `except`）
- [ ] 提供有意義的錯誤訊息
- [ ] 記錄錯誤日誌

#### 日誌

- [ ] 使用 `logging` 模組
- [ ] 關鍵步驟記錄 INFO
- [ ] 錯誤記錄 ERROR + stack trace
- [ ] 敏感資訊不記錄（密碼、token）

**範例**：

```python
import logging
from typing import Dict, Any

logger = logging.getLogger(__name__)

def execute_skill(url: str, timeout: int = 30) -> Dict[str, Any]:
    """Skill 入口函數"""
    try:
        logger.info(f"Starting health check for {url}")

        # 驗證
        if not url.startswith(("http://", "https://")):
            raise ValueError(f"Invalid URL format: {url}")

        # 執行
        result = _perform_check(url, timeout)

        logger.info(f"Health check completed: {result['status']}")
        return result

    except ValueError as e:
        logger.error(f"Validation error: {e}")
        raise
    except Exception as e:
        logger.error(f"Execution error: {e}", exc_info=True)
        raise RuntimeError(f"Health check failed: {str(e)}")
```

### ✅ TypeScript/Stagehand Skills

#### 初始化

- [ ] 正確配置 Stagehand options
- [ ] 設置適當的 timeout
- [ ] headless 模式可配置
- [ ] verbose level 適當

#### 資源管理

- [ ] 使用 try-finally 確保清理
- [ ] `await stagehand.close()` 始終執行
- [ ] 錯誤時保存截圖
- [ ] 長時間操作設置 timeout

#### 錯誤處理

- [ ] 區分不同錯誤類型
- [ ] 提供上下文資訊
- [ ] 返回結構化錯誤
- [ ] 記錄詳細日誌

**範例**：

```typescript
async function executeSkill(options: SkillOptions): Promise<SkillResult> {
  const stagehand = new Stagehand({
    env: "LOCAL",
    headless: options.headless ?? true,
    verbose: 0
  });

  try {
    await stagehand.init();
    const page = stagehand.page;

    await page.goto(options.url, { timeout: options.timeout });
    const result = await performTest(page);

    return { success: true, data: result };

  } catch (error) {
    console.error("Skill failed:", error);

    // 保存調試截圖
    try {
      const screenshot = await page.screenshot({ encoding: "base64" });
      return {
        success: false,
        error: error.message,
        screenshot
      };
    } catch {
      return { success: false, error: error.message };
    }

  } finally {
    await stagehand.close();
  }
}
```

---

## 🧪 測試檢查清單

### ✅ 單元測試

- [ ] 測試參數驗證邏輯
- [ ] 測試錯誤處理路徑
- [ ] 測試邊界條件
- [ ] Mock 外部依賴
- [ ] 測試覆蓋率 > 80%

### ✅ 整合測試

- [ ] 測試與真實服務的互動
- [ ] 測試完整的執行流程
- [ ] 測試超時處理
- [ ] 測試重試機制

### ✅ E2E 測試

- [ ] 測試實際使用場景
- [ ] 測試與其他 Skills 的組合
- [ ] 測試 CI/CD 整合
- [ ] 測試並發執行

---

## 🔒 安全檢查清單

### ✅ 輸入驗證

- [ ] 驗證所有用戶輸入
- [ ] 檢查 URL 格式（防止 SSRF）
- [ ] 限制文件大小/類型
- [ ] 清理特殊字符
- [ ] 使用 Pydantic 進行結構化驗證

### ✅ 密鑰管理

- [ ] 從環境變數讀取密鑰
- [ ] 不在代碼中硬編碼密鑰
- [ ] 不在日誌中記錄密鑰
- [ ] 考慮使用 Vault/Secrets Manager
- [ ] 密鑰加密存儲

### ✅ 權限控制

- [ ] Skills 只能訪問授權資源
- [ ] 使用最小權限原則
- [ ] 限制 Skill 執行範圍
- [ ] 記錄敏感操作審計日誌

---

## 🚀 性能檢查清單

### ✅ 執行效率

- [ ] 單次執行 < 30 秒（大部分情況）
- [ ] 使用連接池（HTTP requests）
- [ ] 實作快取機制（適當時）
- [ ] 避免不必要的重複操作
- [ ] 使用 async/await 提升並發

### ✅ 資源管理

- [ ] 限制記憶體使用
- [ ] 正確關閉連接
- [ ] 清理臨時檔案
- [ ] 避免資源洩漏
- [ ] 設置合理的 timeout

### ✅ 可擴展性

- [ ] 支援並行執行
- [ ] 無狀態設計（適當時）
- [ ] 避免全局變數
- [ ] 線程安全（多線程環境）

---

## 📚 文檔檢查清單

### ✅ SKILL.md

- [ ] 所有章節完整
- [ ] 範例可執行
- [ ] 參數說明清晰
- [ ] 包含錯誤處理說明
- [ ] 標籤正確分類

### ✅ README

- [ ] 安裝步驟清晰
- [ ] 依賴列表完整
- [ ] 快速開始指南
- [ ] 常見問題解答
- [ ] 貢獻指南（開源時）

### ✅ 代碼註釋

- [ ] 公開函數有 docstring
- [ ] 複雜邏輯有註釋
- [ ] 參數和返回值有說明
- [ ] TODO/FIXME 標記清楚

---

## 🔄 CI/CD 檢查清單

### ✅ 自動化測試

- [ ] PR 自動執行測試
- [ ] 測試失敗阻擋合併
- [ ] 覆蓋率報告生成
- [ ] 性能基準測試

### ✅ 代碼品質

- [ ] Linting 自動檢查
- [ ] 格式化自動驗證
- [ ] 類型檢查通過
- [ ] 安全掃描（Bandit/ESLint）

### ✅ 部署流程

- [ ] 自動化部署 pipeline
- [ ] 版本標記清晰
- [ ] 回滾機制就緒
- [ ] 部署後驗證

---

## 📦 發布檢查清單

### ✅ 版本管理

- [ ] 遵循語意化版本（SemVer）
- [ ] CHANGELOG 更新
- [ ] Git tag 創建
- [ ] 版本號一致（code, docs）

### ✅ 文檔更新

- [ ] README 更新
- [ ] API 文檔同步
- [ ] 遷移指南（breaking changes）
- [ ] 範例代碼更新

### ✅ 質量保證

- [ ] 所有測試通過
- [ ] 手動冒煙測試
- [ ] 性能無退化
- [ ] 無已知 critical bugs

---

## 🎯 生產環境檢查清單

### ✅ 監控

- [ ] 日誌聚合（ELK/Loki）
- [ ] 指標收集（Prometheus）
- [ ] 告警配置（Alertmanager）
- [ ] 追蹤設置（Jaeger/Zipkin）

### ✅ 可靠性

- [ ] 錯誤重試機制
- [ ] Circuit Breaker 實作
- [ ] 優雅降級策略
- [ ] 健康檢查端點

### ✅ 可觀測性

- [ ] 結構化日誌
- [ ] 請求追蹤 ID
- [ ] 性能指標暴露
- [ ] 調試端點（非生產）

---

## 📊 性能基準

### ✅ 目標指標

| 指標 | 目標值 | 可接受範圍 |
|------|--------|-----------|
| **執行時間** | < 30s | < 60s |
| **記憶體使用** | < 500MB | < 1GB |
| **成功率** | > 99% | > 95% |
| **並發數** | > 10 | > 5 |
| **錯誤恢復時間** | < 5s | < 10s |

### ✅ 壓力測試

- [ ] 並發執行測試（10+ workers）
- [ ] 長時間運行測試（24+ hours）
- [ ] 資源洩漏檢查
- [ ] 極端條件測試

---

## 🌟 最佳實踐總結

### DO（推薦）

- ✅ 保持 Skills 簡單專注
- ✅ 完整的錯誤處理和日誌
- ✅ 詳盡的文檔和範例
- ✅ 全面的測試覆蓋
- ✅ 版本化和變更追蹤
- ✅ 安全第一的設計

### DON'T（避免）

- ❌ 單一 Skill 做太多事
- ❌ 硬編碼配置值
- ❌ 忽略錯誤處理
- ❌ 缺少日誌記錄
- ❌ 跳過測試
- ❌ 提交密鑰到代碼庫

---

## 📖 參考資源

### 官方文檔

- Claude Skills: https://docs.anthropic.com/claude/docs/skills
- Stagehand: https://docs.stagehand.dev
- MCP: https://modelcontextprotocol.io

### 工具

- **Linting**: Ruff (Python), ESLint (TypeScript)
- **格式化**: Black (Python), Prettier (TypeScript)
- **測試**: pytest (Python), Jest (TypeScript)
- **類型檢查**: MyPy (Python), TypeScript

### 社群

- GitHub Discussions: [repository-url]
- Discord: [invite-link]
- Stack Overflow: tag `claude-skills`

---

## ✅ 快速檢查模板

複製以下清單用於每個新 Skill：

```markdown
## Skill: [Name]

### Design Phase
- [ ] 職責單一清晰
- [ ] SKILL.md 完整
- [ ] 參數設計合理
- [ ] 範例完整可執行

### Implementation Phase
- [ ] 代碼結構清晰
- [ ] 類型註解完整
- [ ] 錯誤處理健全
- [ ] 日誌記錄適當

### Testing Phase
- [ ] 單元測試 > 80%
- [ ] 整合測試通過
- [ ] E2E 測試驗證
- [ ] 性能基準達標

### Documentation Phase
- [ ] SKILL.md 審核
- [ ] README 更新
- [ ] 代碼註釋完整
- [ ] CHANGELOG 更新

### Deployment Phase
- [ ] CI/CD 配置
- [ ] 版本標記
- [ ] 監控設置
- [ ] 告警配置

### ✅ Ready for Production
```

---

**版本**: 1.0.0
**最後更新**: 2025-11-08
**維護者**: Claude (總編輯)
