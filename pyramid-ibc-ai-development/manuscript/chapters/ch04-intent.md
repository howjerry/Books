# 第四章：意圖 (Intent) — 為 AI 設定北極星

**在這一章中，你將學會：**
- 如何用 User Story 格式清楚表達開發意圖
- 業務背景與技術意圖的平衡藝術
- 利用 MCP 從外部工具自動取得需求資訊
- 識別並修正「意圖不清」導致的典型問題

> 💡 **開場白**
>
> 想像你是一個計程車司機。乘客上車後說：「帶我去一個好地方。」
>
> 你會怎麼做？開始隨便開？還是問清楚：「您想去餐廳、酒吧、還是風景區？」
>
> AI 就是那個計程車司機。如果你不告訴它目的地，它只能亂開——技術上它在移動，但不一定是你想去的方向。
>
> **Intent 就是你的目的地。**

---

## 4.1 用 User Story 表達意圖

在軟體開發中，有一個被廣泛使用的格式叫做 **User Story**。它的結構非常簡單：

```
作為一個【角色】，
我希望【功能/行為】，
這樣我就可以【價值/目的】。
```

這個格式之所以經典，是因為它強迫你回答三個關鍵問題：

1. **誰**會使用這個功能？
2. **什麼**是這個功能的具體表現？
3. **為什麼**這個功能有價值？

### User Story 範例

讓我們看幾個例子：

**範例 1：購物車功能**
```
作為一個電商網站的顧客，
我希望能將商品加入購物車，
這樣我就可以一次購買多個商品而不用分開結帳。
```

**範例 2：權限管理**
```
作為一個系統管理員，
我希望能設定不同角色的存取權限，
這樣我就可以確保員工只能存取他們工作需要的資料。
```

**範例 3：效能優化（技術性）**
```
作為一個運維工程師，
我希望 API 的 P95 延遲能降到 200ms 以下，
這樣我就可以在高峰時段維持良好的用戶體驗。
```

> 💡 **小提示**
>
> User Story 不只適用於「用戶功能」。技術性的任務也可以用這個格式——只要把「角色」換成開發者、運維、DBA 等技術人員即可。

---

### 從模糊到清晰

讓我們練習把模糊的需求轉換成 User Story。

**模糊需求 1：**
```
我們需要做一個通知功能
```

**轉換成 User Story：**
```
作為一個電商網站的顧客，
我希望在訂單狀態變更時收到 App 推播通知，
這樣我就可以即時知道包裹的配送進度而不用一直查詢。
```

看到差別了嗎？User Story 版本告訴我們：
- 是給「顧客」用的（不是給內部員工）
- 是「訂單狀態變更」時觸發（不是所有通知）
- 是「App 推播」（不是 email 或 SMS）
- 目的是「即時知道配送進度」（優先處理配送相關通知）

---

**模糊需求 2：**
```
資料庫太慢了，要優化
```

**轉換成 User Story：**
```
作為一個後端工程師，
我希望商品列表查詢的回應時間能從目前的 3 秒降到 500ms，
這樣我就可以讓用戶在瀏覽商品時有流暢的體驗。

【補充背景】
- 目前 products 表有 200 萬筆資料
- 主要的慢查詢是 category_id 過濾 + 價格排序
- 已有 category_id 的索引，但沒有複合索引
```

---

### 🔧 實戰練習：寫出你的 User Story

試著把以下模糊需求轉換成 User Story：

1. 「要做登入功能」
2. 「報表太慢了」
3. 「用戶說找不到想要的商品」

（參考答案在本章末尾）

---

## 4.2 業務背景與技術意圖的平衡

在實際開發中，你會發現意圖可以分成兩層：

```
        ┌─────────────────────────────────┐
        │         業務意圖                 │
        │    （為什麼這件事對業務重要）      │
        └─────────────────┬───────────────┘
                          │
                          ▼
        ┌─────────────────────────────────┐
        │         技術意圖                 │
        │    （為什麼要用這種技術方案）      │
        └─────────────────────────────────┘
```

### 業務意圖 vs 技術意圖

**業務意圖** 回答：這件事對用戶/公司有什麼價值？

**技術意圖** 回答：為什麼選擇這個技術方案而不是其他方案？

讓我們看一個例子：

---

**情境**：你需要為電商網站實作「即時庫存顯示」功能。

**只有業務意圖：**
```
作為一個電商網站的顧客，
我希望能看到商品的即時庫存數量，
這樣我就可以知道商品是否還買得到，避免下單後才被告知缺貨。
```

這很好，但 AI 可能會選擇各種技術方案：
- 每次載入頁面都查資料庫？
- 用快取？多久更新一次？
- 用 WebSocket 即時推送？
- 用輪詢？

**加上技術意圖：**
```
【業務意圖】
作為一個電商網站的顧客，
我希望能看到商品的即時庫存數量，
這樣我就可以知道商品是否還買得到，避免下單後才被告知缺貨。

【技術意圖】
我們選擇 Server-Sent Events (SSE) 而非 WebSocket，原因：
1. 庫存更新是單向的（伺服器 → 客戶端），不需要雙向通訊
2. SSE 更容易實作，不需要維護 WebSocket 連線池
3. 我們的基礎設施已經支援 SSE（前面的通知功能用過）

我們選擇 Redis Pub/Sub 作為庫存變更的訊息傳遞機制，原因：
1. 已有 Redis 基礎設施
2. 可以支援多個 API 伺服器同時運作
3. 庫存變更量不大（約每秒 100 次），Redis 足以應對
```

> 🎯 **實戰技巧**
>
> 什麼時候需要寫技術意圖？
>
> - 當有多種技術方案可選擇時
> - 當你的選擇可能不是「最直覺」的方案時
> - 當團隊有特定的技術偏好或限制時
>
> 技術意圖幫助 AI 理解「為什麼選 A 不選 B」，避免它用你不想要的方式實作。

---

## 4.3 利用 MCP 整合自動取得需求

到目前為止，我們都是「手動」寫意圖。但在實際工作中，需求資訊往往散落在各種工具裡：

- **Linear/Jira**：Issue 的詳細描述
- **Notion/Confluence**：PRD 文件
- **Slack**：討論記錄和決策
- **Figma**：設計稿和互動規格

每次都要手動複製這些資訊，很麻煩對吧？

**這就是 MCP（Model Context Protocol）派上用場的地方。**

### MCP 是什麼？

MCP 是一個讓 Claude Code 能「連接」到外部工具的協定。通過 MCP，你可以讓 Claude Code 直接讀取 Linear、Notion、Slack 等工具的內容。

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Linear    │     │   Notion    │     │   Slack     │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       └───────────────────┼───────────────────┘
                           │
                    ┌──────▼──────┐
                    │  MCP Server │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ Claude Code │
                    └─────────────┘
```

### 實際應用範例

假設你收到一個 Linear ticket：`LIN-1234`

**傳統做法：**
1. 打開 Linear
2. 找到 ticket
3. 複製描述
4. 貼到 Claude Code
5. 開始寫指令

**使用 MCP：**
```
請從 Linear ticket LIN-1234 獲取需求，
然後用 I-B-C 框架幫我設計實作方案。
```

Claude Code 會：
1. 自動讀取 Linear ticket 的標題、描述、留言
2. 理解需求的完整上下文
3. 產出結構化的實作方案

> 📚 **深度閱讀**
>
> MCP 的設定方式請參考附錄 B。本節我們專注於概念和應用場景。

### 更進階的 MCP 應用

**場景 1：跨工具整合**
```
請幫我處理 Linear ticket LIN-1234。
1. 從 Linear 讀取需求描述
2. 從 Notion 的 PRD 文件（連結在 ticket 裡）讀取詳細規格
3. 從 Figma 設計稿了解 UI 細節
4. 實作功能
5. 完成後，更新 Linear ticket 狀態為 "Ready for Review"
```

**場景 2：自動獲取歷史決策**
```
我要修改用戶認證的邏輯。
請先搜尋 Slack #backend-discussions 頻道中
過去三個月關於「認證」的討論，
總結相關的設計決策，
然後再開始修改。
```

**場景 3：需求澄清**
```
Linear ticket LIN-5678 的描述不太清楚。
請幫我在 ticket 留言區詢問 PM 以下問題：
1. 「錯誤訊息」要顯示在哪裡？Modal 還是 inline？
2. 需要支援多語言嗎？
3. 錯誤訊息的文案由誰提供？

等 PM 回覆後，再通知我。
```

> ⚠️ **注意**
>
> MCP 需要適當的權限設定。確保你理解每個 MCP Server 可以存取什麼資料，並遵循公司的資安政策。

---

## 4.4 意圖不清導致的典型問題案例

學會如何「正確表達意圖」之後，讓我們也來看看「意圖不清」會導致什麼問題。這些案例都是真實發生過的。

### 案例 1：方向完全錯誤

**情境：**
```
開發者：幫我寫一個驗證功能
AI：[產出 email 格式驗證的程式碼]
開發者：不是，我要的是用戶身份驗證
AI：[產出 JWT 認證的程式碼]
開發者：不是登入驗證，是驗證用戶提交的資料
AI：[產出表單驗證的程式碼]
開發者：...我是說驗證用戶上傳的身份證照片是否本人
```

**問題分析：**
「驗證」這個詞有太多可能的解讀。開發者腦中想的是「身份驗證（KYC）」，但這是一個非常特定的概念。

**如何避免：**
```
【意圖】
作為一個金融 App 的用戶，
我希望能上傳身份證照片完成實名認證（KYC），
這樣我就可以使用需要實名的功能（如大額轉帳）。

【具體需求】
- 用戶上傳身份證正面和反面照片
- 系統使用 OCR 識別姓名、身份證號
- 與用戶填寫的資料比對
- 可選：使用人臉辨識比對照片與自拍
```

---

### 案例 2：範圍膨脹

**情境：**
```
開發者：幫我做一個簡單的 TODO list
AI：好的，我來幫你實作一個 TODO list 應用...
    - 支援拖放排序
    - 支援標籤分類
    - 支援搜尋
    - 支援多用戶協作
    - 支援提醒通知
    - 支援重複任務
    ...
開發者：等等，我只是要一個簡單的清單啊
```

**問題分析：**
「簡單」是主觀的。對 AI 來說，它可能認為這些都是「TODO list 該有的功能」。

**如何避免：**
```
【意圖】
我需要一個最小可行的 TODO list 原型，用於技術展示。

【明確的範圍限制】
只需要：
- 顯示待辦事項列表
- 新增待辦事項
- 標記為完成
- 刪除待辦事項

不需要：
- 用戶認證
- 資料持久化（頁面重整可以清空）
- 任何進階功能
```

> 💡 **小提示**
>
> 「不需要」的清單跟「需要」的清單一樣重要。
> 明確告訴 AI 什麼**不要做**，可以有效防止範圍膨脹。

---

### 案例 3：遺漏關鍵約束

**情境：**
```
開發者：幫我寫一個批次處理程式，
        每天凌晨處理前一天的訂單資料

AI：[產出程式碼，使用 for 迴圈逐筆處理]

開發者：這太慢了，我們每天有 500 萬筆訂單

AI：[改成多執行緒處理]

開發者：我們的 DB 連線數有限制

AI：[改成連線池]

開發者：而且不能影響正在運行的 API 服務
```

**問題分析：**
開發者沒有在一開始說明資料量、效能要求、系統限制。

**如何避免：**
```
【意圖】
實作每日訂單資料的批次處理程式。

【關鍵約束】
- 資料量：每天約 500 萬筆訂單
- 處理時間窗口：凌晨 2:00-5:00（3 小時）
- 資料庫連線限制：最多使用 10 個連線
- 不能影響其他服務：DB CPU 使用率要控制在 50% 以下
- 必須支援中斷後繼續（斷點續傳）
```

---

## 本章重點回顧

- **要點 1**：User Story 格式（作為...我希望...這樣我就可以...）是表達意圖的有效工具。

- **要點 2**：意圖可以分成「業務意圖」和「技術意圖」。複雜任務需要兩者兼備。

- **要點 3**：MCP 讓你可以自動從 Linear、Notion、Slack 等工具獲取需求資訊。

- **要點 4**：意圖不清會導致方向錯誤、範圍膨脹、遺漏關鍵約束等問題。

---

## 大腦體操 🧠

**問題 1：**
為什麼 User Story 要強調「這樣我就可以...」這個部分？如果省略會怎樣？

**問題 2：**
什麼情況下，只有「業務意圖」是不夠的，還需要補充「技術意圖」？

**問題 3：**
請找出以下指令的問題，並改寫：
```
幫我優化這個查詢，太慢了
```

---

## 練習題參考答案

**「要做登入功能」改寫：**
```
作為一個電商網站的用戶，
我希望能用 email 和密碼登入我的帳號，
這樣我就可以查看我的訂單歷史和管理個人資料。
```

**「報表太慢了」改寫：**
```
作為一個業務分析師，
我希望銷售日報表的產生時間能從目前的 5 分鐘降到 30 秒內，
這樣我就可以在每天早會前準備好最新的銷售數據。
```

**「用戶說找不到想要的商品」改寫：**
```
作為一個電商網站的顧客，
我希望能透過關鍵字搜尋商品，並看到相關度排序的結果，
這樣我就可以快速找到我想買的商品而不用一頁一頁翻。
```

---

## 下一章預告

你已經學會如何表達「為什麼」——清楚的意圖。但光有方向還不夠，你還需要知道「什麼時候算到達」。

在下一章，我們將深入 **Behavior（行為）**——如何定義可驗證的成功標準。你會學到 TDD/BDD 的思維、API 契約測試、以及如何讓 Claude Code 自動驗證它的產出。

準備好定義「什麼叫做完成」了嗎？

---

> 📝 **讀者筆記區**
>
> 寫出一個你目前正在做的功能的 User Story：
>
> 作為一個_____________________，
> 我希望_______________________，
> 這樣我就可以___________________。
>
> 這個功能需要「技術意圖」嗎？如果需要，是什麼？
>
> _________________________________
