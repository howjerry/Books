# 第八章：從 PRD 到可執行任務清單

**在這一章中，你將學會：**
- 用 SMART 原則拆解需求
- 管理任務之間的依賴關係
- 讓 Claude Code 輔助任務拆解
- 大型專案的分階段執行策略

> 💡 **開場白**
>
> 到目前為止，我們學的都是「單一任務」的指令設計。但在真實工作中，你收到的往往是一份 PRD（產品需求文件），裡面有十幾個功能、各種邊界情況、還有一堆「待確認」的項目。
>
> 面對這種情況，你不能一股腦地把整份 PRD 丟給 AI。你需要先把它拆解成**可執行的任務清單**。
>
> **這就是本章要教你的。**

---

## 8.1 需求拆解的 SMART 原則

你可能聽過目標設定的 SMART 原則。同樣的原則也適用於任務拆解。

### SMART 是什麼？

| 字母 | 代表 | 在任務拆解中的意義 |
|------|------|-------------------|
| **S** | Specific（具體） | 任務描述要清楚，沒有模糊空間 |
| **M** | Measurable（可衡量） | 有明確的完成標準 |
| **A** | Achievable（可達成） | 在合理時間內可以完成 |
| **R** | Relevant（相關） | 直接貢獻於最終目標 |
| **T** | Time-bound（有時限） | 有明確的預估時間 |

### 好任務 vs 壞任務

讓我們看一些例子：

**❌ 壞任務**

```
1. 做登入功能
2. 處理用戶資料
3. 優化效能
```

這些任務的問題：
- 「做登入功能」太籠統，不知道包含什麼
- 「處理用戶資料」不具體，不知道是建立、讀取、還是刪除
- 「優化效能」沒有完成標準

**✅ 好任務**

```
1. [API] 實作 POST /api/auth/login 端點
   - 輸入：email, password
   - 輸出：JWT access_token
   - 包含：輸入驗證、錯誤處理、測試案例
   - 預估：2 小時

2. [API] 實作 GET /api/users/me 端點
   - 輸入：JWT token（在 header）
   - 輸出：當前用戶資料
   - 包含：認證中介層、測試案例
   - 預估：1 小時

3. [優化] 將商品列表查詢的 P95 延遲從 500ms 降到 100ms
   - 方法：加入 Redis 快取
   - 驗證：用 k6 跑負載測試
   - 預估：3 小時
```

這些任務：
- **具體**：明確說明是什麼 API、什麼輸入輸出
- **可衡量**：有測試案例、有效能指標
- **可達成**：每個任務都在幾小時內可完成
- **相關**：直接對應到需求
- **有時限**：有預估時間

> 💡 **小提示**
>
> 一個好的經驗法則：**每個任務應該在 2-4 小時內可以完成**。
>
> 如果一個任務預估需要 8 小時以上，那就應該繼續拆分。

---

## 8.2 任務依賴關係管理

任務之間不是獨立的，它們有先後順序和依賴關係。

### 依賴關係類型

```
1. 順序依賴（Sequential）
   任務 B 必須等任務 A 完成才能開始

   [A: 建立 User 模型] → [B: 建立 User API]

2. 部分依賴（Partial）
   任務 B 只需要任務 A 的一部分就能開始

   [A: 建立 User 模型 + API]
         ↓（模型完成後）
   [B: 建立 Order 模型]（需要 User 模型做 FK）

3. 無依賴（Independent）
   可以同時進行

   [A: 建立商品 API] ⟷ [B: 建立分類 API]
```

### 依賴圖範例

假設我們要實作一個簡單的電商功能，包含：用戶、商品、訂單。

```
                    ┌─────────────────┐
                    │  1. 專案設定     │
                    │  (資料庫、框架)  │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        ▼                    ▼                    ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│ 2. User 模型  │    │ 3. Product 模型│    │ 4. Category 模型│
└───────┬───────┘    └───────┬───────┘    └───────┬───────┘
        │                    │                    │
        │            ┌───────┴────────────────────┘
        │            │
        ▼            ▼
┌───────────────┐    ┌───────────────┐
│ 5. User API   │    │ 6. Product API │
│ (需要 User 模型)│    │ (需要 Product + │
└───────┬───────┘    │  Category 模型) │
        │            └───────┬────────┘
        │                    │
        └─────────┬──────────┘
                  ▼
          ┌───────────────┐
          │ 7. Order 模型  │
          │ (需要 User +   │
          │  Product FK)  │
          └───────┬───────┘
                  │
                  ▼
          ┌───────────────┐
          │ 8. Order API   │
          └───────────────┘
```

### 用清單表示依賴

```
任務清單：

1. [基礎] 專案設定
   - 依賴：無
   - 預估：1 小時

2. [模型] User 模型
   - 依賴：任務 1
   - 預估：0.5 小時

3. [模型] Product 模型
   - 依賴：任務 1
   - 預估：0.5 小時

4. [模型] Category 模型
   - 依賴：任務 1
   - 預估：0.5 小時

5. [API] User API (CRUD)
   - 依賴：任務 2
   - 預估：2 小時

6. [API] Product API (CRUD)
   - 依賴：任務 3, 4
   - 預估：2 小時

7. [模型] Order 模型
   - 依賴：任務 2, 3
   - 預估：1 小時

8. [API] Order API
   - 依賴：任務 5, 6, 7
   - 預估：3 小時
```

> ⚠️ **注意**
>
> 依賴管理很重要！如果你讓 AI 先做任務 8（Order API），它會因為缺少 User 和 Product 模型而產出不完整或錯誤的程式碼。

---

## 8.3 讓 Claude Code 輔助任務拆解

好消息：Claude Code 本身就可以幫你拆解任務！

### 讓 AI 分析 PRD

```
我有一份 PRD，請幫我拆解成可執行的任務清單。

要求：
1. 每個任務要符合 SMART 原則
2. 標示任務之間的依賴關係
3. 估算每個任務的工時
4. 按照執行順序排列

【PRD 內容】
{貼上你的 PRD}
```

### 實際範例

**輸入的 PRD：**

```
## 會員等級系統

### 功能描述
根據會員的消費金額，自動計算並更新會員等級。
等級影響折扣和專屬優惠。

### 等級規則
- 銅卡：累積消費 $0 - $9,999
- 銀卡：累積消費 $10,000 - $49,999
- 金卡：累積消費 $50,000 - $199,999
- 白金卡：累積消費 $200,000+

### 等級權益
- 銅卡：無折扣
- 銀卡：95 折
- 金卡：9 折 + 免運
- 白金卡：85 折 + 免運 + 專屬客服

### 等級計算時機
- 每次訂單完成後
- 每月 1 日重新計算

### 其他需求
- 要有等級變動通知
- 管理後台可以查看等級分布
- API 給 App 查詢當前等級
```

**Claude Code 產出的任務清單：**

```
## 會員等級系統 - 任務清單

### Phase 1: 基礎架構（依賴：無）

1. [模型] MemberLevel 列舉和 MemberTier 模型
   - 內容：
     - 定義 MemberLevel 列舉（BRONZE, SILVER, GOLD, PLATINUM）
     - 建立 member_tiers 設定表（等級、門檻、折扣、權益）
     - 在 users 表加入 member_level, total_spent 欄位
   - 預估：1 小時
   - 依賴：無

2. [服務] MemberLevelService 基礎
   - 內容：
     - calculate_level(total_spent) 方法
     - get_benefits(level) 方法
   - 測試：等級計算的單元測試
   - 預估：1 小時
   - 依賴：任務 1

### Phase 2: 核心功能（依賴：Phase 1）

3. [服務] 訂單完成時更新等級
   - 內容：
     - 在 OrderService.complete_order 後觸發
     - 更新 user.total_spent
     - 重新計算等級
   - 測試：訂單完成後等級升級
   - 預估：1.5 小時
   - 依賴：任務 2

4. [任務] 每月等級重算背景任務
   - 內容：
     - Celery 定時任務
     - 批次處理所有用戶
     - 效能考量（100 萬用戶）
   - 測試：批次處理正確性
   - 預估：2 小時
   - 依賴：任務 2

5. [服務] 折扣計算整合
   - 內容：
     - 在購物車/結帳流程套用等級折扣
     - 金卡以上免運邏輯
   - 測試：不同等級的折扣計算
   - 預估：1.5 小時
   - 依賴：任務 2

### Phase 3: 通知和 API（依賴：Phase 2）

6. [服務] 等級變動通知
   - 內容：
     - 升級/降級時發送通知（App 推播 + Email）
     - 通知模板
   - 測試：通知觸發邏輯
   - 預估：1.5 小時
   - 依賴：任務 3

7. [API] GET /api/member/level
   - 內容：
     - 當前等級和權益
     - 距離下一等級的金額
     - 等級歷史
   - 測試：API 回應格式
   - 預估：1 小時
   - 依賴：任務 2

### Phase 4: 管理後台（依賴：Phase 2）

8. [API] 管理後台 - 等級分布統計
   - 內容：
     - 各等級人數
     - 等級變動趨勢
   - 預估：1 小時
   - 依賴：任務 2

9. [API] 管理後台 - 手動調整等級
   - 內容：
     - 管理員可手動升/降級
     - 記錄調整原因
   - 預估：1 小時
   - 依賴：任務 2

---

## 總覽

| Phase | 任務數 | 總工時 |
|-------|--------|--------|
| Phase 1 | 2 | 2 小時 |
| Phase 2 | 3 | 5 小時 |
| Phase 3 | 2 | 2.5 小時 |
| Phase 4 | 2 | 2 小時 |
| **總計** | **9** | **11.5 小時** |

## 建議執行順序

1 → 2 → (3, 4, 5 可並行) → (6, 7 可並行) → (8, 9 可並行)
```

---

## 8.4 大型專案的分階段執行策略

對於大型專案（幾十個甚至上百個任務），你需要更系統化的管理方式。

### 階段劃分策略

```
┌─────────────────────────────────────────────────────────────┐
│                         大型專案                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Sprint 1: MVP（最小可行產品）                               │
│  ├── 核心功能                                               │
│  └── 基本流程可以跑通                                        │
│                                                             │
│  Sprint 2: 完善                                             │
│  ├── 錯誤處理                                               │
│  ├── 邊界情況                                               │
│  └── 效能優化                                               │
│                                                             │
│  Sprint 3: 進階功能                                         │
│  ├── 次要功能                                               │
│  ├── 管理後台                                               │
│  └── 報表/分析                                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 使用 Claude Code 管理多階段專案

**策略一：每個階段一個對話**

```
對話 1（Sprint 1）：
- 完成核心功能
- 確認可以運作
- 提交程式碼

對話 2（Sprint 2）：
- 讀取對話 1 的產出
- 加入錯誤處理
- 優化效能

對話 3（Sprint 3）：
- 讀取之前的產出
- 加入進階功能
```

**策略二：使用任務清單檔案**

在專案中建立 `TASKS.md`：

```markdown
# 專案任務清單

## Sprint 1: MVP
- [x] 任務 1: User 模型和 API
- [x] 任務 2: Product 模型和 API
- [ ] 任務 3: Order 模型和 API ← 進行中

## Sprint 2: 完善
- [ ] 任務 4: 輸入驗證
- [ ] 任務 5: 錯誤處理
- [ ] 任務 6: 效能優化

## Sprint 3: 進階
- [ ] 任務 7: 搜尋功能
- [ ] 任務 8: 推薦系統
```

然後告訴 Claude Code：

```
請參考 TASKS.md，繼續進行下一個未完成的任務。
完成後，更新 TASKS.md 的狀態。
```

**策略三：使用 Session 檔案**

建立 `.claude/session.md` 記錄當前狀態：

```markdown
# 當前開發狀態

## 已完成
- User CRUD API（routers/users.py）
- Product CRUD API（routers/products.py）

## 進行中
- Order API - 已完成模型，API 待實作

## 待處理
- 購物車功能
- 結帳流程

## 重要決策
- 訂單狀態使用字串而非數字（參考 ADR-003）
- 庫存使用樂觀鎖（避免死鎖）

## 已知問題
- Product API 的分頁效能需優化（任務 6）
```

---

## 本章重點回顧

- **要點 1**：用 SMART 原則拆解任務——具體、可衡量、可達成、相關、有時限。

- **要點 2**：管理任務依賴關係，確保任務按正確順序執行。

- **要點 3**：Claude Code 可以幫你分析 PRD 並產出任務清單。

- **要點 4**：大型專案要分階段執行（MVP → 完善 → 進階），並用檔案管理狀態。

---

## 大腦體操 🧠

**問題 1：**
以下任務描述符合 SMART 原則嗎？如果不符合，如何改進？
```
優化搜尋功能，讓它更好用
```

**問題 2：**
給定以下任務，畫出依賴關係圖：
- A: 建立資料庫 schema
- B: 建立 API 端點
- C: 撰寫前端頁面
- D: 整合測試
- E: 部署到測試環境

**問題 3：**
你正在開發一個部落格系統，PRD 包含：用戶管理、文章管理、留言功能、標籤系統、搜尋功能。請用 SMART 原則拆解成任務清單。

---

## 下一章預告

任務拆解完成後，你開始一個一個執行。但 AI 產出的程式碼不一定是完美的——它可能有 bug、效能問題、或不符合最佳實踐。

在下一章，我們將學習**迭代修正與品質守門**——如何有效地 review AI 的產出、如何給建設性的回饋、以及如何建立品質檢查清單。

準備好成為 AI 的「品質守門員」了嗎？

---

> 📝 **讀者筆記區**
>
> 你目前手上最大的專案有多少個功能點？
>
> _________________________________
>
> 試著用 SMART 原則拆解其中一個功能：
>
> _________________________________
