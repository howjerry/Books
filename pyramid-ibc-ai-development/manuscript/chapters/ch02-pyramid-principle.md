# 第二章：金字塔原則在程式開發的應用

**在這一章中，你將學會：**
- 什麼是金字塔原則，為什麼它改變了全球商業溝通
- 如何將「結論先行」的思維應用到 AI 指令設計
- 避免「發散式指令」的常見陷阱
- 設計有邏輯層次的提示結構

> 💡 **開場白**
>
> 1973 年，麥肯錫顧問公司的芭芭拉・乏乏（Barbara Minto）發表了一本改變商業溝通的著作——《金字塔原理》。這本書的核心觀點簡單到令人驚訝：**先說結論，再說原因**。
>
> 五十年後，這個原則意外地成為了「指揮 AI」的最佳實踐。讓我們來看看為什麼。

---

## 2.1 什麼是金字塔原則

在我們談 AI 之前，讓我先講一個故事。

### 📖 兩種報告的故事

想像你是一個專案經理，早上開會前收到兩封來自團隊成員的報告。

**報告 A：**

```
昨天我花了三小時研究用戶投訴的問題。
首先，我查看了伺服器日誌，發現有一些異常的 timeout。
然後，我檢查了資料庫連線池的設定。
接著，我分析了最近一週的流量數據。
後來，我發現 Redis 的記憶體使用率很高。
我又追查了 Redis 的 key 分布。
最後我發現是快取沒有設定過期時間，
導致 Redis 記憶體溢出，
進而影響了 API 的回應時間。
我建議我們添加 TTL 設定。
```

**報告 B：**

```
【結論】
API 回應變慢是因為 Redis 記憶體溢出，建議立即添加 TTL 設定。

【原因】
1. 快取 key 沒有設定過期時間
2. 一週內累積了 50 萬個無用的 key
3. Redis 記憶體使用率達到 98%

【依據】
- 伺服器日誌顯示 timeout 集中在 Redis 相關操作
- Redis 監控顯示記憶體持續成長
- 手動清理測試後，回應時間恢復正常

【行動建議】
1. 立即：為所有快取 key 添加 24 小時 TTL
2. 短期：建立 Redis 記憶體監控告警
3. 長期：Review 所有快取策略
```

**你更喜歡哪一份報告？**

如果你跟大多數人一樣，你會選擇報告 B。為什麼？

---

### 金字塔結構的威力

報告 B 使用了**金字塔結構**：

```
                    ┌───────────────┐
                    │   主要結論     │
                    │ (一句話答案)   │
                    └───────┬───────┘
                            │
          ┌─────────────────┼─────────────────┐
          ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │  支持論點1 │      │  支持論點2 │      │  支持論點3 │
    └─────┬────┘      └─────┬────┘      └─────┬────┘
          │                 │                 │
    ┌─────┴─────┐     ┌─────┴─────┐     ┌─────┴─────┐
    │  證據/細節  │     │  證據/細節  │     │  證據/細節  │
    └───────────┘     └───────────┘     └───────────┘
```

**金字塔原則的核心思想：**

1. **結論先行**：先說最重要的答案，再補充細節
2. **分組歸納**：相關的想法要組織在一起
3. **邏輯遞進**：從上到下是「為什麼」，從下到上是「所以」
4. **MECE 原則**：各組之間互不重疊、整體完整（Mutually Exclusive, Collectively Exhaustive）

> 📚 **深度閱讀**
>
> Barbara Minto 的《金字塔原理》（The Pyramid Principle）至今仍是麥肯錫、BCG 等頂尖顧問公司的必讀教材。如果你想深入了解這個框架，強烈建議閱讀原著。

---

## 2.2 從商業寫作到指令設計

現在，讓我們把鏡頭轉向 AI。

### 為什麼 AI 需要金字塔原則？

還記得第一章的「超級聰明實習生」比喻嗎？AI 就像一個什麼都會、但什麼都不知道的實習生。

當你給它一個「發散式」的指令時，它會：
1. 猜測你想要什麼
2. 基於猜測做決定
3. 產出可能完全偏離你期望的結果

**想像一下這個場景：**

```
你：幫我處理一下用戶資料的問題

AI 心裡在想：
- 「處理」是什麼意思？清理？驗證？轉換？加密？
- 「用戶資料」指的是哪些欄位？
- 「問題」是什麼問題？格式？效能？安全性？
- 他想要我寫程式碼？還是給建議？
算了，我就選一個最可能的情況來回答好了...
```

結果：AI 給了你一段「資料清理」的程式碼，但你其實是想問「為什麼用戶資料會重複」。

### 金字塔原則如何幫助你

當你使用金字塔結構來設計指令時，你是在**幫 AI 排除不確定性**。

**改進後的指令：**

```
【結論/目標】
我需要找出並解決用戶資料重複的問題。

【背景/原因】
- 最近一週收到多起用戶投訴，說收到重複的行銷郵件
- 初步檢查發現 users 表有 email 相同但 id 不同的記錄
- 這影響了我們的用戶統計準確性

【預期產出】
1. SQL 查詢：找出所有重複的用戶記錄
2. 分析報告：重複發生的可能原因
3. 解決方案：如何合併重複記錄並防止未來再發生

【限制條件】
- 資料庫是 PostgreSQL 15
- users 表大約有 500 萬筆記錄
- 不能刪除任何訂單歷史關聯
```

看到差別了嗎？這個指令告訴 AI：
- **你要什麼**（結論先行）
- **為什麼要這個**（背景支持）
- **完成是什麼樣子**（預期產出）
- **有什麼限制**（邊界條件）

---

## 2.3 結論先行的提示結構

讓我們把這個概念具體化成一個可以重複使用的結構。

### 🎯 金字塔提示模板

```
【目標】（一句話說明你要什麼）
___________________________________

【背景】（為什麼需要這個）
- 原因 1
- 原因 2
- 原因 3

【預期行為】（完成後是什麼樣子）
- 輸入：___
- 輸出：___
- 邊界情況：___

【技術上下文】（在什麼環境下）
- 語言/框架：___
- 現有程式碼位置：___
- 相關檔案：___

【限制與偏好】（什麼不能做/偏好什麼做法）
- 必須：___
- 禁止：___
- 偏好：___
```

### 實際範例：重構一個函數

讓我們用這個模板來設計一個實際的指令。

**情境**：你有一個 200 行的「上帝函數」，做了太多事情，想要重構它。

**使用金字塔模板：**

```
【目標】
將 src/services/order_service.py 中的 process_order 函數
（目前 200 行）重構成多個小函數，每個函數職責單一。

【背景】
- 這個函數是三年前寫的，隨著業務增長不斷被添加新功能
- 目前包含：驗證、庫存檢查、價格計算、折扣應用、建立訂單、發送通知
- 新人很難理解這段程式碼
- 任何小修改都容易引發意外的 bug

【預期行為】
重構後：
- 每個函數不超過 30 行
- 函數命名清楚表達其職責
- 原有的 15 個測試案例必須全部通過
- 函數之間的呼叫順序要有清楚的文件說明

【技術上下文】
- Python 3.11 + FastAPI
- 現有測試在 tests/services/test_order_service.py
- 使用 SQLAlchemy 2.0 的 ORM
- 依賴注入使用 dependency-injector

【限制與偏好】
- 必須：保持向後相容，process_order 函數簽名不能改
- 必須：每個新函數都要有 type hints
- 禁止：不要引入新的外部套件
- 偏好：使用 private method（底線開頭）作為內部函數
```

> 💡 **小提示**
>
> 你不需要每次都填滿這個模板的每一項。這個模板是用來**提醒你思考**可能遺漏的面向。根據任務的複雜度，有時候只需要填前兩項就夠了。

---

## 2.4 避免發散式指令的常見陷阱

知道「該怎麼做」很重要，但知道「不該怎麼做」也同樣重要。讓我們來看看一些常見的反模式。

### ❌ 陷阱 1：問題太開放

**糟糕的指令：**
```
幫我改善這段程式碼
```

**為什麼這是問題？**
「改善」可以有一百種解讀：
- 效能優化？
- 可讀性提升？
- 安全性加強？
- 測試覆蓋？
- 減少記憶體使用？

**改進版本：**
```
【目標】
提升 src/utils/data_processor.py 中 transform_data 函數的執行效能。

【背景】
目前處理 10 萬筆資料需要 45 秒，希望降到 10 秒以內。
Profiling 顯示瓶頸在第 45-60 行的巢狀迴圈。
```

---

### ❌ 陷阱 2：假設 AI 知道你的上下文

**糟糕的指令：**
```
把上次說的那個功能做完
```

**為什麼這是問題？**
- AI 沒有「記憶」（除非你在同一個對話中）
- 即使在同一個對話，「那個功能」也太模糊

**改進版本：**
```
【目標】
完成用戶登入功能的錯誤處理部分。

【上下文】
前面我們已經完成了 src/routers/auth.py 中的基本登入邏輯。
現在需要添加以下錯誤處理：
1. 帳號不存在時回傳 404
2. 密碼錯誤時回傳 401
3. 帳號被鎖定時回傳 403
```

---

### ❌ 陷阱 3：一次問太多不相關的事

**糟糕的指令：**
```
幫我：
1. 修復登入 bug
2. 優化首頁載入速度
3. 更新文件
4. 設定 CI/CD
```

**為什麼這是問題？**
- 這四件事完全沒有關聯
- AI 需要頻繁切換上下文
- 任何一個出錯都會影響其他的

**改進版本：**

分成四個獨立的對話或指令，每個都遵循金字塔結構。

或者，如果你真的想一次處理多件事：

```
【目標】
按照以下順序完成四個獨立任務。每完成一個任務，
請停下來確認，我說「繼續」後再進行下一個。

【任務清單】
1. 修復登入 bug（詳見下方 Task 1）
2. 優化首頁載入速度（詳見下方 Task 2）
3. 更新文件（詳見下方 Task 3）
4. 設定 CI/CD（詳見下方 Task 4）

---
## Task 1: 修復登入 bug
【目標】...
【背景】...
---
## Task 2: 優化首頁載入速度
...
```

---

### ❌ 陷阱 4：只說「不要」，不說「要」

**糟糕的指令：**
```
不要用 for 迴圈，不要用全域變數，不要寫太長的函數
```

**為什麼這是問題？**
這就像跟人說「不要想大象」——你腦中已經有大象了。

告訴 AI「不要什麼」不如告訴它「要什麼」來得有效。

**改進版本：**
```
【編碼風格偏好】
- 優先使用 list comprehension 和 map/filter
- 所有狀態都通過函數參數傳遞
- 每個函數控制在 20 行以內，超過就拆分
```

---

## 🔧 實戰練習：改寫發散式指令

讓我們來練習一下！

### 練習題

以下是三個「發散式指令」，請用金字塔原則改寫它們。

**指令 1：**
```
幫我寫一個 API
```

**指令 2：**
```
這段程式碼有問題，幫我看一下
```

**指令 3：**
```
我們的系統最近很慢，幫我優化
```

> 🚀 **試試看**
>
> 先自己改寫這三個指令，然後對照下方的參考答案。
> 記住：沒有標準答案，重點是你的改寫是否遵循了金字塔原則。

### 參考答案

**指令 1 改寫：**
```
【目標】
建立一個商品查詢 API，讓前端可以根據分類瀏覽商品列表。

【預期行為】
- GET /api/v1/products?category={category_id}&page={n}&limit={m}
- 回傳：{ "items": [...], "total": number, "page": number }
- 支援分頁，預設每頁 20 筆

【技術上下文】
- FastAPI + SQLAlchemy
- 商品模型已存在於 models/product.py
- 參考現有的 users API（routers/users.py）的風格
```

**指令 2 改寫：**
```
【目標】
找出並修復 src/services/payment.py 第 45-60 行的 bug。

【問題描述】
當用戶使用信用卡付款但餘額不足時，系統沒有正確回滾交易，
導致訂單狀態顯示「已付款」但實際上沒有扣款成功。

【重現步驟】
1. 建立一個金額 $1000 的訂單
2. 使用測試信用卡（餘額 $500）付款
3. 觀察訂單狀態和 payment_transactions 表

【預期 vs 實際】
- 預期：訂單狀態應為「付款失敗」
- 實際：訂單狀態為「已付款」
```

**指令 3 改寫：**
```
【目標】
分析並優化系統回應時間，目標是將 P95 延遲從目前的 2 秒降到 500ms 以下。

【背景】
- 過去一週用戶投訴增加 300%
- Datadog 監控顯示 API 延遲從 200ms 上升到 2000ms
- 問題在每天下午 2-4 點特別嚴重（流量高峰）

【已知資訊】
- 伺服器 CPU 使用率正常（約 40%）
- 資料庫連線數經常達到上限（100 連線）
- Redis 記憶體使用率 85%

【需要你做的】
1. 分析可能的瓶頸點
2. 提供優化建議，按照影響程度排序
3. 對最高優先級的優化，提供具體的實作方案
```

---

## 本章重點回顧

- **要點 1**：金字塔原則的核心是「結論先行」——先說你要什麼，再解釋為什麼。

- **要點 2**：對 AI 來說，結構化的指令 = 減少不確定性 = 更準確的輸出。

- **要點 3**：金字塔提示模板包含五個部分：目標、背景、預期行為、技術上下文、限制與偏好。

- **要點 4**：避免四大陷阱：問題太開放、假設 AI 知道上下文、一次問太多不相關的事、只說「不要」不說「要」。

---

## 大腦體操 🧠

**問題 1：**
為什麼「結論先行」對 AI 特別重要？（提示：想想 AI 是如何「閱讀」你的指令的）

**問題 2：**
MECE 原則（互不重疊、整體完整）如何應用在設計 AI 指令上？舉一個例子。

**問題 3：**
以下指令有什麼問題？如何改進？
```
幫我把這個功能做得更好一點，之前用戶有抱怨
```

---

## 下一章預告

金字塔原則給了我們一個「結構」，但這個結構裡應該放什麼內容呢？

在下一章，我們將介紹本書的核心框架——**I-B-C**（Intent-Behavior-Context）。你會學到如何精準定義「意圖」、如何設計「可驗證的行為」、如何提供「足夠的上下文」。

這不只是一個理論框架，而是一套你每天都會用到的實戰工具。準備好了嗎？

---

> 📝 **讀者筆記區**
>
> 你之前寫過的最「發散」的 AI 指令是什麼？現在你會怎麼改寫它？
>
> _________________________________
>
> 金字塔原則讓你想到工作中的哪些溝通場景？
>
> _________________________________
>
> 你覺得「結論先行」最難的部分是什麼？
>
> _________________________________
